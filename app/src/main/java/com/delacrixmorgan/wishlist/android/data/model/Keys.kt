package com.delacrixmorgan.wishlist.android.data.model

class Keys {
    enum class BudgetDetail {
        BudgetId
    }

    enum class BudgetOverview {
        OverviewType
    }

    enum class Form {
        BudgetId
    }

    enum class History {
        BudgetId,
        DateType
    }

    enum class CreateCompletion {
        BudgetId
    }

    enum class ActionBottomSheet {
        ActionType
    }

    enum class EditBottomSheet {
        Title
    }

    enum class ConfirmationBottomSheet {
        Title
    }
}